// To find the area of the circle
function circle(radius)
{    
    const res1=Math.PI*(radius**2)
    console.log("");
    console.log(`The area of a circle given radius ${radius}  is ${res1}`);
}
circle(2);


// to check if a number is positive, negative, or zero.
function