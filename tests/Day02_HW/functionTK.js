// To find the area of the circle
function circle(radius) {
    var res1 = Math.PI * (Math.pow(radius, 2));
    console.log("The area of a circle given radius ".concat(radius, "  is ").concat(res1));
}
circle(2);
